# Figma parity — what we claim, who owns it, and the test that proves it

This repo has one recurring failure mode: a decision gets taken twice, the two
copies drift, and the owner finds the drift by *looking at the screen* — a page
name painted over the artwork, a right-panel row that will not select, a status
line that is unreadable. Every fix in that pattern started with "which of the two
owners is right?".

So the rule is now mechanical instead of remembered:

1. **One owner per decision.** A design decision lives in exactly one place.
   `tools/design-sheet/guard.mjs` fails the gate when a hardcoded canvas colour
   appears in two files, when a new literal appears without a declared ceiling,
   or when the chrome asks for an icon the icon set does not have.
2. **A behaviour is a test, not a sentence.** Every Figma behaviour we claim
   below names the test that pins it, and the guard fails if that test no longer
   exists. A behaviour with no test is listed as *open*, never quietly dropped.
3. **A decision that is visual is checked on pixels.** `raster.rs` owns a
   tiny-skia sink, so canvas chrome is asserted by rasterizing it and reading the
   pixels back (no GPU, runs in the ordinary gate). See
   `canvas_pixels_name_the_outermost_frames_and_never_the_page`.

The **master list** — every tool, gesture, panel, key and format we have, each row
matched to Figma with a status — is
[docs/FIGMA_PARITY_MASTER_LIST.md](FIGMA_PARITY_MASTER_LIST.md). It is the work
queue: an increment either closes a row there or it does not happen. The sections
below are the rows already strong enough to name an owner and a test.

## 1. The owners

| decision | owner |
| --- | --- |
| the app's role palette (Graphite + Daylight), text/background/state colours | `tools/design-sheet/tokens.mjs` → `x-ui` → `every_role_named_here_is_a_role_the_palette_defines` |
| window chrome colours and row geometry (panels, dashboard, popovers) | `x-ui` roles + the ratchet in `apps/x-designer/src/bin/x_native_app/design_tokens_test.rs` |
| the canvas renderer's chrome: name labels, placeholders, board grids | `crates/x-render/src/ir.rs` (`LABEL_ABOVE_Y`, `LABEL_SIZE`, `label_ink()`) and the ceilings in `tools/design-sheet/guard.mjs` |
| which canvas name is painted at all | `ir.rs::child_in_frame` + the same walk in `crates/x-render/src/scene.rs` |
| the page payload vs the page list | `run.rs::commit_page_rename`, `App::delete_page`, `pages_rows` |

**Adding a Figma behaviour, start to finish:**

```
1. put the rule in its one owner (never at the call site);
2. write the test that pins it, named after the behaviour;
3. add the row below with the Figma URL and that test name;
4. node tools/design-sheet/guard.mjs   # fails if the row and the test disagree
```

## 2. What we say we copy from Figma

| behaviour | Figma source | our rule (owner) | pinned by |
| --- | --- | --- | --- |
| **Clean up layers** (chapter-4 framing, owner-confirmed 2026-09-20): flatten redundant nests — a `Group` with exactly one child that carries no opacity/blend/effect/mask is unwrapped, bottom-up, positions preserved, one undo entry; rename stays manual | [FD4B: Clean up your layers](https://help.figma.com/hc/en-us/articles/30979556779159) | `x-editor/src/editor_core.rs::clean_up_layers` + `clean_node` / `redundant_group`, `CtxCmd::CleanupLayers`, right-click + palette "Clean up layers" | `clean_up_layers_flattens_redundant_nests_in_one_undo` |
| The chrome's UI type is Figma's: **Inter on an 11 px base** — every label, field value, row, menu item and tracked section heading sits on one step, and the face is read back from each bundled file's own `name` table instead of trusted from the stem it was registered under | owner report, 2026-09-20 (Figma's chrome re-measured for the Wave-2 pass; the forum reads the same interface at 11 px — [forum.figma.com/t/36463](https://forum.figma.com/suggest-a-feature-11/change-interface-scale-or-just-figma-s-ui-font-scale-36463)) | `theme.rs::T_UI` (an alias of `T11`, the chrome's body step), `paint.rs::micro_label` / `caps_label`, `TextUi::load`'s four bundled Inter weights | `the_chrome_type_is_figmas_inter_at_eleven_pixels` |
| The **Star** and **Arrow** tool glyphs are the shapes those tools draw — the star at Figma's default inner-point ratio (0.382, the value this app's Star tool defaults to), the arrow as a shaft ending in the head the shape menu's arrow draws, not Lucide's corner bracket; the Frame glyph's `#` is keyed `frame-hash` so the sheet's extractor (a `[a-z0-9-]` vocabulary) can draw it; and the icon census now also reads the `fn icon()` / `kind_icon` binding tables, so the "unused" list is a real number instead of a blind spot | [Access design tools from the toolbar](https://help.figma.com/hc/en-us/articles/360041064174), [Shape tools](https://help.figma.com/hc/en-us/articles/360040450133) | `icons.rs` glyph table (star / arrow-up-right / frame-hash), `state.rs::kind_icon` (Slice layer wears the Slice tool's glyph, not Figma's Cut scissors), `tools/design-sheet/icon_scan.mjs` | `the_star_and_arrow_glyphs_are_figmas_metaphors` |
| The **Comment** glyph is Figma's rounded-square speech bubble with its tail at the bottom-left — one closed outline that actually drops to a corner — not Lucide's circular chat bubble | [Access design tools from the toolbar](https://help.figma.com/hc/en-us/articles/360041064174) | `icons.rs` glyph table (the comment key redrawn to a tailed rounded square) | `the_comment_glyph_is_a_tailed_bubble` |
| Canvas cursors follow Figma's system-cursor vocabulary: an I-beam over text — including the Text tool armed on the canvas — a crosshair over the geometry tools, grab/grabbing for Hand and pan, ew-resize on the panel splitter, and pointer on controls | [Access design tools from the toolbar](https://help.figma.com/hc/en-us/articles/360041064174) | `run.rs::cursor_for` | `the_text_tool_wears_an_i_beam` |
| Effect adds stop at Figma's per-type caps — eight drop shadows, eight inner shadows, one layer blur, two noise effects, one background blur per layer (help 360041488473) — enforced in `Editor::add_effect_layer` so the panel can no longer stack without limit | [Apply effects to layers](https://help.figma.com/hc/en-us/articles/360041488473) | `x-core::EffectKind::limit`, `x-editor::Editor::add_effect_layer` | `effect_adds_stop_at_figmas_per_type_caps` |
| `N` zooms to the next top-level frame and `⇧N` to the previous one, wrapping around the page and naming the frame it landed on | [Keyboard shortcuts](https://help.figma.com/hc/en-us/articles/360040328653) | `run.rs::zoom_to_frame` | `n_walks_the_pages_frames_in_order` |
| A ⇧? keyboard-shortcuts sheet opens over the canvas, lists the keys, and stays open so you can try them; it is a flat list, not Figma's tabbed panel that live-highlights the keys you have used (PARTIAL) | [Keyboard shortcuts](https://help.figma.com/hc/en-us/articles/360040328653) | `editor_ui::paint_shortcuts_panel` | `the_shortcut_sheet_and_the_two_hide_ui_keys` |
| `⇧D` enters Figma's Dev Mode by switching the right panel to the Inspect (SHIP) view; a second `⇧D` returns to Design (help 360039956914) | [Dev Mode](https://help.figma.com/hc/en-us/articles/360039956914) | `run.rs::on_character` | `shift_d_enters_and_leaves_dev_mode` |
| The rotation field counts **counter-clockwise positive** (Figma) while the engine stores the renderer's y-down clockwise angle: one display conversion at the single `Sel.rot` source, inverse on write, stored sign unchanged | [Rotate an object](https://help.figma.com/hc/en-us/articles/360039956914) | `state.rs::rotation_display` / `rotation_from_display`, `editor_ui.rs::sel_info` (single readout source), `run.rs` Rotation write | `the_rotation_field_shows_figmas_counter_clockwise_sign` |
| Chrome corner radii follow Figma's measured steps: 8 px rows and cards, 6 px inputs, 4 px chips | owner report, 2026-09-20 (re-measured Figma chrome for the Wave-2 pass; master list §18 preamble — not a published token) | `theme.rs` aliases `R_ROW`/`R_CARD`=8, `R_INPUT`=6, `R_PILL`=4 on the shared `RadiusScale` | `radii_follow_figmas_measured_chrome` |
| A click selects the top-level object; a double-click goes one level down; ⌘/Ctrl-click is the deep one-press select | [Select layers and objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `selection.rs::top_level_ancestor`, `editor_core.rs::click_select` / `drill_into`, `run.rs::on_press` | `figma_click_selects_top_level_then_drills` |
| Shift-click adds a layer to the selection | [Select layers and objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `editor_core.rs::click_select` (shift toggles membership) | `shift_click_adds_and_a_second_shift_click_removes_from_the_selection` |
| A second Shift-click on the same layer removes it again | [Select layers and objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `editor_core.rs::click_select` | `shift_click_adds_and_a_second_shift_click_removes_from_the_selection` |
| A marquee drag on empty canvas answers with the page's top-level objects | [Selection marquee](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `selection.rs::hit_test_rect(.., deep=false)`, `editor_core.rs::marquee` | `command_drag_marquee_reaches_nested_layers_while_a_plain_drag_stops_at_the_frame`, `a_plain_marquee_stops_at_the_page_top_level_and_the_deep_one_does_not` |
| Holding ⌘/Ctrl and dragging the marquee reaches layers nested inside a frame | [Selection marquee](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `selection.rs::hit_test_rect(.., deep=true)`, `editor_core.rs::marquee_deep`, `Drag::Marquee { deep }` | `command_drag_marquee_reaches_nested_layers_while_a_plain_drag_stops_at_the_frame`, `a_plain_marquee_stops_at_the_page_top_level_and_the_deep_one_does_not` |
| **Select matching layers (⌥⌘A)**: the same layer — by name and place, not by size — in the page's other frames and groups, and only within the same Section | [Select matching objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `editor_core.rs::find_matching_nodes` (name+structure, section-scoped), `Action::SelectMatching` | `select_matching_finds_the_same_layer_and_never_crosses_a_section`, `select_matching_never_crosses_a_section_boundary`, `option_command_a_selects_the_matching_layer_in_the_other_frame` |
| ⏎ selects one level down, ⇧⏎ the parent, ⇥/⇧⇥ the next/previous sibling — and the page is not a layer to walk up into | [Select layers and objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `Action::SelectChild` / `SelectParent` / `SelectNextSibling` / `SelectPrevSibling` | `enter_tab_and_shift_enter_walk_the_layers_the_way_figma_documents` |
| **An instance's More-actions menu** carries *"Go to main component"*, *"Push changes to main component"* (both need the master in this file) and a **Reset flyout** that *"only lists properties that have changes applied"* | [Apply changes to instances](https://help.figma.com/hc/en-us/articles/360039150733), [Edit main components](https://help.figma.com/hc/en-us/articles/360038665934) | `x-core/src/components.rs::instance_changes` (stable list) + `push_overrides_to_master` (appearance only), `context_menu.rs::build_menu_items` (rows) | `the_canvas_menu_carries_figmas_instance_actions`, `pushing_changes_to_main_and_resetting_one_change_are_undoable` |
| **A layer inside an instance** is reached by double-clicking it and edited from there: *"you can change the properties of any layer within an instance"*; **position, constraints and text bounds cannot be overridden**; one layer carries one override in this engine (the `.x` string form), where Figma keeps each property side by side | [Apply changes to instances](https://help.figma.com/hc/en-us/articles/360039150733) | `x-editor/src/editor_core.rs::enter_instance` / `exit_instance` / `scoped_layer` (the instance is swapped for its resolved subtree to answer the point, and the four property writers store an override), `selection.rs::instance_ancestor` + `replace_in_tree`, `run.rs` (double-click enters, Esc leaves), `context_menu`-style breadcrumb from `App::enter_instance_status` | `selecting_inside_an_instance_picks_the_layer_under_the_cursor`, `editing_inside_an_instance_stores_an_override`, `position_is_not_overridable_inside_an_instance`, `a_second_write_on_the_same_layer_replaces_its_override`, `the_panel_shows_the_instance_copy_of_a_layer_inside_it` |
| An instance's internals carry no names | [Instance names are not displayed](https://forum.figma.com/t/why-isnt-the-name-of-an-instance-being-displayed/34665) | instance seeding sets `in_frame` | `slot_content_replaces_anchor_in_rendered_instance` |
| A **frame** name is canvas chrome and never part of an export, while a **Section's** title chip exports with the section | [Section titles export?](https://forum.figma.com/t/section-titles-are-exporting-as-part-of-the-image/13338) | `x-native/src/export.rs`: `/label` is stripped, `/pill` + `/chip` are kept | `export_strips_frame_names_and_keeps_a_sections_title_chip`, `t13_canvas_and_export_must_encode_the_same_fill_stack` |
| A Section's title is a **filled chip in the section's own colour** above its top-left corner — not a grey frame-style label — sized to the name it carries | [Show names of nested frames](https://forum.figma.com/t/show-names-of-nested-frames/41949) | `ir.rs::SECTION_PILL_*` + `section_pill_rect` (the one geometry rule), the Section arms of both encoders, `x_core::section_hue()` | `a_section_name_is_a_chip_sized_to_the_name`, `canvas_pixels_name_the_outermost_frames_and_never_the_page` |
| A frame carries its own **"Show name"** switch in the right sidebar's Layer section — the third and last gate on a frame label, after "not the root" and "not nested in a frame" | [Hide frame names in Figma](https://designilo.com/figma/how-to-hide-frame-name-in-figma/) | `Node::show_name`, the frame arms of `ir.rs` and `scene.rs`, `Action::ToggleShowName` (inspector, frames only), `Editor::set_show_name` | `a_frame_that_switches_its_name_off_emits_no_label`, `the_show_name_row_toggles_a_frame_name_undoably_and_only_for_frames`, `show_name_roundtrips_and_defaults_to_true_for_older_files` |
| A Group answers a marquee like any other layer — it has bounds; only a *click* passes through its empty area | [Select layers and objects](https://help.figma.com/hc/en-us/articles/360040449873-Select-layers-and-objects) | `selection.rs::hit_test_rect` (no Group exception), `hit_test` (a Group has no body) | `a_group_answers_a_marquee_like_any_other_layer` |
| A frame's name is chrome on the canvas: in the band above the frame, aligned to its left edge, never on the artwork | [Frame names on the canvas](https://designilo.com/figma/how-to-hide-frame-name-in-figma/) | `ir.rs::LABEL_ABOVE_Y`, `LABEL_SIZE` + the same constants in `scene.rs` | `a_frame_name_labels_the_gutter_above_it_and_never_the_root`, `canvas_pixels_name_the_outermost_frames_and_never_the_page` |
| Only a page's outermost frame is named; a frame nested in another frame is silent; a Section is always named and the frames inside it keep theirs | [Show names of nested frames](https://forum.figma.com/t/show-names-of-nested-frames/41949) | `ir.rs::child_in_frame` + `scene.rs` | `a_frame_name_labels_the_gutter_above_it_and_never_the_root`, `canvas_pixels_name_the_outermost_frames_and_never_the_page` |
| The page — the render root — is never named; its name lives in the page list | owner report, 2026-09-18 | `ir.rs` root guard (`!in_frame` is false for a root that has no parent), `scene.rs` | `canvas_pixels_name_the_outermost_frames_and_never_the_page`, `a_frame_name_labels_the_gutter_above_it_and_never_the_root` |
| A name is drawn in one colour, the same on the canvas and in an export, and fades only with the node's own opacity | owner report, 2026-09-18 (the 70%-cap drift) | `ir.rs::label_ink()`, the single owner for all four label sites | `canvas_pixels_name_the_outermost_frames_and_never_the_page` |
| The canvas name band and the page hit-zone are one geometry: the page's own top-left band, 26px above it, 18px tall | owner report, 2026-09-18 | `ir.rs::LABEL_ABOVE_Y` / `LABEL_SIZE`; the page band in `editor_ui.rs` | `every_page_in_the_window_is_clickable_and_the_window_follows_the_page` |
| Pages are a list in the left sidebar: click selects, double-click renames, the menu deletes | [Create and manage pages](https://help.figma.com/hc/en-us/articles/360038511293-Create-and-manage-pages) | `run.rs::pages_rows`, `commit_page_rename`, `App::delete_page` | `pages_add_switch_delete`, `double_click_on_a_page_name_opens_its_rename_field` |
| Renaming a page renames the page — never a frame | owner report, 2026-09-18 | `run.rs::commit_page_rename` (writes the list and the editor root) | `page_rename_names_the_page_and_mirrors_the_root`, `page_rename_survives_save_and_reload` |
| Deleting a page deletes the page that was aimed at; the last page is refused | [Create and manage pages](https://help.figma.com/hc/en-us/articles/360038511293-Create-and-manage-pages) | `run.rs::App::delete_page` | `page_delete_removes_the_clicked_page_and_never_the_last_one` |
| Every page is reachable in the list, however many there are (Figma scrolls its page list) | [Create and manage pages](https://help.figma.com/hc/en-us/articles/360038511293-Create-and-manage-pages) | `run.rs::pages_rows` + the `PAGES_MAX_ROWS` window | `pages_list_windows_beyond_four_pages_and_keeps_every_page_reachable`, `pages_past_the_fourth_are_selectable_and_deletable`, `every_page_in_the_window_is_clickable_and_the_window_follows_the_page` |
| Page edits, comments and page reordering are one undo history | owner report, 2026-09-18 | `x-editor` history | `document_history_orders_page_edits_comments_and_reordering` |
| A double-click on a panel row acts once: the popover the first press opened is not shut by the second | [Component properties](https://help.figma.com/hc/en-us/articles/5579474826519-Explore-component-properties) | `run.rs::App::is_repeat_chrome_click` (350 ms / 4 px) | `double_click_on_a_panel_toggle_counts_once` |
| Double-clicking a name renames it where it is written (page rows, layer rows) | [Create and manage pages](https://help.figma.com/hc/en-us/articles/360038511293-Create-and-manage-pages) | `FieldId::LayerName`, `Action::LayerRename` | `double_click_on_a_layer_name_renames_it`, `double_click_on_a_page_name_opens_its_rename_field` |
| A panel row's height and its hit zone are the same box, so a click cannot land in a gap | owner report, 2026-09-18 | `editor_ui.rs` row geometry | `app_row_heights_are_the_component_layers` |
| Clicking inside the text field you are already editing moves the caret; it does not re-open the field and throw the typed buffer away | owner report, 2026-09-18 | `run.rs::on_press` returns from the in-editor caret branch before any re-seed | `pressing_inside_the_open_text_field_keeps_what_was_typed` |
| A new layer joins the container you DRAW IT IN — the deepest visible, unlocked frame or section under the point the drag started from (a nested frame beats its parent; a group or an instance never captures) | [Frames in Figma Design](https://help.figma.com/hc/en-us/articles/360041539473-Frames-in-Figma-Design) (*"Click inside an existing frame to add a 100 x 100 nested frame"*), [Shape tools](https://help.figma.com/hc/en-us/articles/360040450133-Shape-tools) | `run.rs::container_under` + `finish_create` | `a_shape_drawn_over_a_frame_joins_that_frame`, `the_deepest_container_wins_and_a_group_does_not_capture`, `a_hidden_or_locked_frame_does_not_take_the_shape` |
| Space while dragging prevents nesting: the new layer stays on the page even when it is drawn over a frame | [Figma tip](https://twitter.com/figmadesign/status/1229872874325831680) (*"To prevent an object from being nested, hold the Spacebar while dragging"*) | `run.rs::finish_create` reads `space_pan` | `holding_space_while_drawing_keeps_the_shape_on_the_page` |
| Shape-tool modifiers: ⇧ constrains to "perfect squares, circles and polygons" and ⌥ draws from the centre, and the pending shape shows its size while dragging | [Shape tools](https://help.figma.com/hc/en-us/articles/360040450133-Shape-tools) | `state.rs::create_rect` is the ONE rule the live preview (`editor_ui.rs`) and `finish_create` share | `shape_tool_modifiers_build_the_rect_the_preview_shows` |
| The **Scale tool (K)** — `K` selects it, `V`/`Esc` go back to Move — resizes proportionally and takes the layer's own distances with it — stroke weight, corner radius, text size, effect offsets and auto-layout padding/gap — and a locked layer is refused | [Scale layers while maintaining proportions](https://help.figma.com/hc/en-us/articles/360040451453-Scale-layers-while-maintaining-proportions) | `Tool::Scale` + `state::scale_drag_factor` / `scaled_box` (one rule for the drag preview and the commit); `Editor::scale_nodes_about` (one undo step) | `the_scale_tool_grows_a_layer_from_the_corner_you_are_not_holding`, `scale_tool_takes_text_effects_and_layout_with_it` |
| A scale is uniform and pinned to the corner you are NOT holding, and a multi-layer selection scales against ONE combined box | [Scale layers while maintaining proportions](https://help.figma.com/hc/en-us/articles/360040451453-Scale-layers-while-maintaining-proportions) | `Editor::scale_nodes_about(parts, factor)` — the anchor is the fixed point of the mapping, so it never moves; nested ids are never scaled twice | `scale_about_an_anchor_pins_it_and_is_one_undo_step` |
| The **Scale panel** (K) is the right sidebar's Scale section: a **scale multiplier** ("type a multiplier in the text field and press Enter"), **width and height fields** where "the other dimension field will automatically update", and a **nine-point anchor box** that "tells Figma which side of the object to stay put" — and the layer's **bounding box itself is draggable** ("Hover over the object's bounding box to make the cursor appear. Then, click-and-drag to resize") | [Scale layers while maintaining proportions](https://help.figma.com/hc/en-us/articles/360040451453-Scale-layers-while-maintaining-proportions) | `editor_ui::paint_scale_block` paints it only while the Scale tool is active; `FieldId::ScaleFactor`/`ScaleW`/`ScaleH` commit through `Editor::scale_nodes_about`; `state::scale_cell_anchor` is the nine-cell table behind `Action::ScaleCell`; `Drag::ScaleBody` uses `state::scale_grab_factor`, whose handle-grab case is `state::corner_point` | `the_scale_tables_are_figmas_nine_points`, `the_scale_panel_and_the_body_drag_scale_the_selection` |
| An **ellipse carries arc properties**: hovering shows a single **Arc handle** "on the right-hand side", dragging it changes the **sweep** (up = positive percentage, down = negative), and **Start** (the handle "with a dot inside it") and **Ratio** ("at the center of the circle", which turns it into a ring) appear beside it — the same three in the sidebar's Appearance section. The change is **non-destructive**: "the shape's bounding box stayed the same size to preserve space in case we wanted to change the arc again", and Flatten is what makes the box hug the geometry | [Arc tool](https://help.figma.com/hc/en-us/articles/360040450173-Arc-tool-create-arcs-semi-circles-and-rings) · [FD4B: Turn an ellipse into an arc](https://help.figma.com/hc/en-us/articles/31130312631191-FD4B-Turn-an-ellipse-into-an-arc) | `x_core::booleans::arc_path_cmds(w, h, start, end, ratio)` is the one closed wedge/ring outline every drawer reads, with `arc_point` shared by the geometry and the handles; `state::arc_handles` is the one handle table (paint, hit test and drag), `Drag::ArcHandle` writes appearance only — never the box — and `FieldId::ArcStart`/`ArcSweep`/`ArcRatio` are the same properties by typing | `the_arc_handles_and_fields_turn_an_ellipse_into_a_ring`, `a_quarter_arc_is_a_wedge_through_the_centre`, `a_ratio_cuts_a_ring_out_of_the_wedge`, `arc_full_circle_and_arbitrary_sweep`, `arc_roundtrips_through_x_format` |
| A **prototype connection is drawn on the canvas**: on the Prototype tab a blue circle sits on the selected layer's edge, "hover over the blue circle … until a blue plus icon appears", drag it to another frame and "Figma will snap the connection noodle to the [frame] when you get close enough. Release your cursor to complete the connection" — the frame the flow starts from then carries "a small blue label … named Flow 1", and a connection "you can select … and press Delete to remove it" | [FD4B: Add prototype connections](https://help.figma.com/hc/en-us/articles/31011968186007-FD4B-Add-prototype-connections) | `editor_ui::page_connections` is the one list (canvas, Delete and the flow label all read it), `conn_anchor_point` is Figma's placement on the layer's right edge, `frame_under` is the snap, `flow_name` numbers the flows in page order, and `Drag::ProtoConnect` writes the same `Interaction::click` the sidebar's Add button does | `the_plus_on_a_layers_edge_draws_a_connection` |
| The **Prototype panel's interaction row** names the action and points at the destination — Figma's editor reads "Trigger: On drag", "Action: Navigate to", "Destination: footer_section", "Animation: Move In", **Direction ← → ↓ ↑**, "Easing: Ease out", "Duration: 300ms" — and the four direction arrows set the side a Move in / Move out enters from | owner report, 2026-09-19 (the owner's screenshots of Figma's interaction editor, [FD4B](https://help.figma.com/hc/en-us/articles/31011968186007-FD4B-Add-prototype-connections)) | `editor_ui::paint_prototype` (action-type pill, the arrow and the destination control), `proto_action_type_label`, `run.rs::proto_direction_set` | `the_prototype_row_names_the_action_and_its_arrows_set_the_side` |
| **⌥⌘G (Frame selection)** wraps the selection in a new Frame sized to the members' collective bounds, works with a single layer, and keeps their positions | [Frames in Figma Design](https://help.figma.com/hc/en-us/articles/360041539473-Frames-in-Figma-Design) | `CtxCmd::FrameSelection` → `Editor::frame_selection` → `Command::FrameSelection` (`wrap_selection` builds the AABB and re-offsets the members) | `option_command_g_wraps_the_selection_in_a_frame_like_figma`, `frame_selection_wraps_and_is_undoable`, `frame_selection_wraps_single_node` |
| The **Pencil tool (⇧P)** draws freehand — "Click and drag on the canvas to sketch" — "with a round 3px stroke weight", and it "stays active until you select another tool or press Esc" | [Sketch on the canvas with the pencil tool](https://help.figma.com/hc/en-us/articles/4402723791511-Sketch-on-the-canvas-with-the-pencil-tool) | `Tool::Pencil` + `freehand_path` (RDP simplify, then a Catmull-Rom pass) turns the sampled stroke into editable curves: one insert is one undo step, the ink and weight are one source for the preview and the node, the stroke joins the frame it was drawn in, and ⇧ while drawing gives the straight line the same page tips | `the_pencil_draws_a_smoothed_stroke`, `samples_become_smooth_cubics`, `the_wobble_a_bigger_eps_cannot_see_is_dropped`, `two_points_are_a_path_and_fewer_are_not` |
| The **Slice tool (S)** draws an export region: "a specific region of the screen for export, even if it's not organized into a single group", and with Contents Only off "anything that overlaps the slice will be exported" | [Using the Slice Tool](https://help.figma.com/hc/en-us/articles/360040028394-Using-the-Slice-Tool), [Export from Figma Design](https://help.figma.com/hc/en-us/articles/360040028114-Export-from-Figma-Design) | `Tool::Slice` + `NodeKind::Slice`: the editor draws the region dashed with its name, and `prepare_export` builds the region's tree through `x_render::ir::build_render_tree_slice` — one slice per export, an empty slice still exporting its size, and a mixed selection refusing to guess | `the_slice_tool_draws_an_export_region`, `a_slice_exports_the_content_inside_its_bounds` |
| **Present** starts a presentation of the file's prototype, and a presentation paints the artwork alone — no frame names, no section chips, no canvas chrome at all | [Figma Design for beginners](https://help.figma.com/hc/en-us/sections/30880632542743) (the toolbar's Present) · [Show frame name in prototype](https://forum.figma.com/suggest-a-feature-11/show-frame-name-in-prototype-32523) | `FrameCache::set_presenting` + `ir::strip_canvas_chrome`; the → `Action::FlowEnter` control and `run.rs::canvas_scene` | `the_header_play_button_presents_the_prototype`, `a_presentation_strips_the_canvas_chrome_and_keeps_the_artwork`, `presenting_invalidates_the_cached_scene` |
| A layer row carries Figma's eye and padlock: they appear on the row you hover, stay while the state is on, and a locked layer stops answering the canvas | [Figma Design for beginners](https://help.figma.com/hc/en-us/sections/30880632542743) · [Figma interface — basic information](https://firmbee.com/figma-interface-basic-information-figma-for-beginners-2) | `editor_ui.rs` row toggles (`Action::TreeVisible` / `Action::TreeLock`), `x-editor::hit_test` | `a_layer_row_hides_and_locks_the_layer_like_figmas_eye_and_padlock` |
| A status message gets a row of its own: the chrome reserves the window's bottom band for it, no danger fill is painted in the chrome, and the flow viewer paints no band at all | owner report, 2026-09-18 | `state.rs::status_band` (the one rect) + `paints_status_band`, `run.rs::paint_feedback` (the one painter) | `the_status_band_is_chrome_and_the_artwork_stops_above_it`, `the_status_band_carries_the_running_jobs_controls`, `the_flow_viewer_paints_no_status_band_over_the_prototype` |
| **Constraints** on a layer inside a frame: the Design tab shows two dropdowns — the first manages the layer's horizontal position, the second its vertical one (Left / Right / Left & Right / Center / Scale, Top / Bottom / Top & Bottom / Center / Scale) — and resizing the frame then carries the layer with it | [Figma Design for beginners](https://help.figma.com/hc/en-us/sections/30880632542743) (ch. 4, "Frame presets and constraints") · [Designlab Figma 101, day 6](https://designlab.com/figma-101-course/create-a-tablet-or-desktop-version) (*"Constraints define how any object will behave if its containing Frame is resized"*, *"change the Constraints settings to \"Left & Right\" and \"Bottom\""*) | `state.rs::CONSTRAINT_H` / `CONSTRAINT_V` is the one table and `editor_ui.rs` paints it; `x_editor::pin_deltas` is the one solver, with `apply_constraints` (inspector W/H, in place) and `pin_commands` (the frame's corner drag, as commands) as its two writers — both hand a changed child's resize on to a nested frame and neither answers for a group | `the_pin_table_is_figmas_own`, `a_nested_frame_hands_the_resize_to_its_own_children`, `the_commands_and_the_in_place_pass_agree`, `a_frame_hands_the_corner_drag_to_its_pinned_children`, `a_group_drag_leaves_its_children_alone`, `constraints_carry_a_frames_layers_through_its_resize` |
| The **Brush** sits beside the Pencil in Figma Draw and paints a textured mark — use the Pencil "for quick sketches, notes, and line work, then enhance your creations with the Brush tool to add texture and color for a more organic, hand-painted appearance"; the stroke's style comes from the secondary toolbar, and brush styles also live in the right sidebar's advanced stroke settings; `Shift` draws a straight line, and both tools "automatically place vector points along the path you draw" | [Draw with illustration tools](https://help.figma.com/hc/en-us/articles/31440438150935-Draw-with-illustration-tools) | `Tool::Brush` (⇧B, design-only) samples the pencil's gesture and lands `x_core::brush_outline` — a CLOSED outline whose ends taper and whose two edges carry a deterministic grain, i.e. the brush style as an outline rather than a source shape stretched along the stroke — with `state::BrushStyle` (Ink / Marker / Dry) as the one table the preview, the panel's BRUSH STYLE block and the landed layer all read; the layer's box is the mark's own (`x_core::path_bounds`), and one mark is one undo step | `the_brush_paints_a_mark`, `a_mark_is_a_closed_band_around_the_spine`, `taper_thins_both_ends`, `grain_roughens_the_edges_and_is_never_random`, `fewer_than_two_points_is_not_a_mark`, `the_box_wraps_the_geometry_and_shifting_moves_it` |
| The **Line and Arrow tools** share one key in the shape menu: **L** draws a line "in any direction" — a straight segment whose paint is the Stroke section — and **⇧L** the **Arrow**, the same segment ending in a solid head | [Shape tools](https://help.figma.com/hc/en-us/articles/360040450133-Shape-tools) | `Tool::Line` / `Tool::Arrow` (L / ⇧L, design-only like the Scale, Slice, Pencil and Brush tools): `x_core::line_path` is the two endpoints and `x_core::arrow_path` closes the spine with a filled head — `4 × weight` long, never under 12 units, never past the far end — and both re-origin onto `x_core::path_bounds`, so a horizontal line's box is 0 units high; ⌥ draws from the centre, ⇧ snaps the drag's direction to 45° steps, and `x_editor::hit_test` answers a stroke-only path by the distance to its ink | `the_line_and_arrow_tools_draw_stroked_paths`, `a_line_is_the_segment_between_its_endpoints`, `a_horizontal_line_is_a_box_with_no_height`, `the_head_sits_on_the_end_and_scales_with_the_weight`, `a_head_never_grows_past_a_short_segment`, `a_degenerate_arrow_is_a_line`, `hit_test_reaches_a_thin_lines_ink` |
| The **Polygon and Star** are two shapes in the shape menu that count their own geometry: a polygon is "an enclosed shape that is made up of any number of straight lines" whose "default shape … is a triangle", a star is "polygons that are arranged in a star shape" that ships as "a five pointed star with ten sides" and carries **Count** ("the minimum is three and the maximum is 60"), **Ratio** ("the distance of the inner points of the star from the center … as percentage of the star's diameter") and **Radius** — and the box around them "is below the bottom of the shape … to remain a consistent shape or size, when additional points are added" | [Shape tools](https://help.figma.com/hc/en-us/articles/360040450133-Shape-tools) | `x_core::booleans::poly_path_cmds` / `star_path_cmds` is the ONE outline (through `ring_cmds` + `arc_point`, clamped by `COUNT_MIN..COUNT_MAX`), and its kind arms feed the renderer, `path_hit`, the SVG/Figma/Sketch exporters and codegen; `state::shape_handles` is the one handle table (paint, hit test and drag), `Drag::ShapeHandle` writes appearance only — never the box — and `FieldId::ShapeCount` / `StarRatio` are the same numbers by typing. **Not built:** the star's Radius handle and Edit-object point editing | `the_polygon_and_star_tools_count_their_sides`, `a_polygon_is_a_ring_of_sides_whose_first_is_the_top`, `a_star_alternates_outside_points_with_ratio_inside_ones`, `hit_test_answers_on_a_polygons_and_stars_ink`, `polygon_and_star_roundtrip_through_x_format` |
| The Prototype tab's **Scroll behavior** block: a frame carries an **Overflow** menu — *No scrolling / Horizontal / Vertical / Both directions* — and an object on a frame that scrolls carries a **Position** menu — *Scroll with parent / Fixed / Sticky*; the preview then scrolls the frame with the wheel inside its box, and an interaction that asks to reset loads the next screen "from the top of the frame" | [Prototype scroll and overflow behavior](https://help.figma.com/hc/en-us/articles/360039818734) | `state.rs`'s `PROTO_OVERFLOW_LABELS`/`PROTO_OVERFLOW_VALUES` and `PROTO_POSITION_LABELS`/`PROTO_POSITION_VALUES` are the tables the field AND its menu both read (one owner per label), `proto_overflow_for_row` owns "No scrolling → Clip", `scrollable_ancestor` owns "Position needs a scrolling frame above the layer"; `x_core::scroll_extent` owns the range (fixed and sticky children excluded), `Editor::set_scroll_position` is the one undoable write and `Editor::set_scroll_preview` the preview-owned offset, driven by `flow_scroll_wheel`, `flow_clear_scroll` and `flow_pan_to`'s in-frame branch | `the_scroll_behaviour_rows_write_the_frames_overflow_and_a_layers_position`, `scroll_position_is_a_document_edit_and_the_preview_offset_is_not`, `content_past_the_frame_decides_the_range`, `the_position_menu_reads_and_writes_the_two_flags`, `fixed_and_sticky_children_do_not_extend_the_range` |
| **Animate matching layers**: a tick in the interaction's animation section — on, the two screens' layers are matched by **name and hierarchy**, the matches smart-animate their differences, a layer that matched nothing dissolves in, and a matched **fixed** layer gets no transition at all; Figma gives overlay actions no smart animate | [Smart animate layers between frames](https://help.figma.com/hc/en-us/articles/360039818874) | `x_core::prototype::matching_layers` owns the rule (a path of ancestor names → `SmartAnimate { from }` / `Dissolve` / `Hold`) and `x_core::smart_animate::interpolate_matching_layers` the in-between state; `Interaction::animate_matching_layers` is the flag, on the file as "smartmatch"; the panel's tick is `state::PROTO_MATCHING_LABEL`, written through `proto_toggle_matching`; `x_native::editor::arm_smart_tick` freezes the plan at the navigation and `SmartTick` is the clock the viewer repaints on, dissolving the arriving layers at `SmartTick::layer_alpha` | `matching_layers_follows_names_hierarchy_and_fixed`, `matching_layers_interpolate_by_name_not_id`, `the_matching_layers_tick_survives_the_round_trip_and_stays_off_when_absent`, `the_interaction_row_carries_figmas_matching_layers_tick`, `the_matching_layers_tick_arms_on_a_navigation_and_dissolves_new_layers` |
| The Prototype tab's **trigger control** is a dropdown whose entries are Figma's own words — *On click / On tap*, *While hovering*, *While pressing*, *Mouse enter*, *Mouse leave*, *Mouse down / Touch press*, *Mouse up / Touch release*, *On drag*, *After delay*, *Key/Gamepad*, *When video hits*, *When video ends* — and *Mouse down* is the press itself: permanent and one-way, where *While pressing* reverts when the press ends | [Connect your prototype](https://help.figma.com/hc/en-us/articles/360040315773-Connect-your-prototype) | `Trigger::label` is the ONE owner of those words (the panel's duplicate table is deleted), `Trigger::all` is the menu and `Trigger::row` its tick, `Trigger::to_str` the wire name the reader now knows for all twelve; the player fires Mouse down on the press between While pressing and On click and never arms the while-span auto-reverse for it | `the_trigger_words_are_figmas_and_the_menu_lists_every_one`, `every_trigger_word_and_its_parameter_survive_the_round_trip`, `the_trigger_menu_offers_figmas_list_and_the_pill_fits_its_words`, `player_mouse_down_navigates_on_press_and_release_keeps_it` |
| An auto-layout frame's **Width**/**Height** dropdown carries the min/max dimensions: *"Minimum and maximum dimensions is an additional setting that can be used at the same time as other resizing properties"*, set with **Add min width** / **Add max width** (and the height pair), edited in the field that appears, marked on the axis icon — *"its respective width or height icon will gain two lines, one on each side"* — and removed with **Remove min and max** | [Guide to auto layout](https://help.figma.com/hc/en-us/articles/360040451373) | `App::modify_selected_layout` is the one writer: `Action::LayoutAxisMenu` is the dropdown, `AddAxisLimit` / `ClearAxisLimits` its min/max rows, `FieldId::MinWidth` / `MaxWidth` / `MinHeight` / `MaxHeight` the fields, and `AutoLayout::{min,max}_{width,height}` are clamped by `apply_auto_layout` for EVERY sizing (the padding floor still wins) | `min_and_max_dimensions_clamp_either_sizing`, `the_width_menu_carries_figmas_sizing_and_min_max_rows`, `a_min_and_max_width_are_added_from_the_menu_and_clamp_the_frame` |
| The **Effects** section is a list: add one of Figma's types from the `+` (*"The **Drop shadow** effect is selected by default. Use the dropdown to switch to **Inner shadow**"*), hide an individual effect, edit the settings the *Effect settings* discloses — **X**, **Y**, **Blur**, the shadow's **Fill**, a blur's **Radius**, noise's **Density** — duplicate it, and reorder it by dragging a row | [Apply effects to layers](https://help.figma.com/hc/en-us/articles/360041488473) | `x_core::Effect::{kind, fields, field, set_field, color, set_color}` describe what each type carries and `EffectKind::{all, label, icon}` own the five types in Figma's order, so the panel builds every row from the model and a type can neither grow nor lose a field; `Editor::{set_effect_kind, set_effect_field, set_effect_color, set_effect_layer_visible, duplicate_effect_layer, set_effect_layer_blend}` are the writers and `effect_at` makes each refuse — before any undo entry — when the row is gone; `mutate_visual_stack` mirrors the ordered stack into the legacy `effects` list, so every render path paints the order the panel shows; `Drag::EffectRow` is the reorder | `the_effects_list_is_the_stack_and_every_write_is_one_undo_step`, `an_effect_write_past_the_end_changes_nothing`, `only_a_shadow_carries_a_fill`, `the_effect_kinds_are_figmas_five_with_their_own_fields`, `the_effects_section_lists_every_effect_with_figmas_controls`, `dragging_an_effect_row_reorders_the_stack`, `an_effect_colour_never_lands_on_the_layer_fill` |
| **Blend modes** are available to *"an entire layer, individual fills, and certain types of effects"*: **Pass through** leads the layer list and is *"the default mode for layers"*, and *"Pass through cannot be applied to fills or effects"*; the other 18 modes carry Figma's own words and order | [Apply blend modes to layers, fills, and effects](https://help.figma.com/hc/en-us/articles/360040667874) | `BlendKind::{label, layer_modes, paint_modes, row_in}` is the ONE owner — the layer menu is the paint menu with Pass through in front, and the two lists differ by exactly the row the documentation names; `Editor::{set_layer_blend, set_paint_layer_blend, set_effect_layer_blend}` write them and refuse Pass through where Figma does; the Appearance row, the colour popover's *Apply blend mode* row and every effect row read those lists | `the_blend_lists_are_figmas_and_pass_through_is_layer_only`, `pass_through_is_a_layer_mode_only`, `the_blend_menus_offer_figmas_modes_and_write_the_choice` |
| A **component set** is a frame that holds nothing but components: *"all components in a set must share the same slash-naming structure"* and *"a set can contain only components"*; its default look is a dashed violet stroke with no fill; it reads as ONE item in the tree whose variants are named by their value, and **Add variant** lives on the set | [Create and use variants](https://help.figma.com/hc/en-us/articles/360056440594) | `x_core::variant_set_members` / `is_variant_set` is the ONE owner of "is this frame a set" (every child a `Component` named `Set/Variant`, one set prefix); `Editor::combine_as_variants` builds the frame — reusing one that already holds only the selection — moves the masters in and renames them in ONE undo entry through `rename_master_in` / `rename_component_refs`; `paint_variant_chrome` draws the outline and the name chip, and `C_SET` is the themed alias so no theme can leave the violet stale | `combining_two_masters_builds_a_set_frame_that_holds_them`, `a_frame_holding_only_the_selection_becomes_the_set`, `a_set_is_all_variants_and_nothing_else`, `a_component_set_reads_as_one_row_and_its_variants_by_value`, `a_component_set_paints_its_dashed_outline_and_name` |
| **Canvas stacking**: *"When multiple layers have negative spacing creating a stack, the last object … will be on top by default"*, and the auto-layout settings offer **First on top** / **Last on top** instead — *"the order of layers in the layers panel stays the same. Canvas stacking is solely a visual change that happens on the canvas."* | [Use the horizontal and vertical flows](https://help.figma.com/hc/en-us/articles/31289464393751) | `x_core::auto_layout::paint_order` + `paints_first_on_top` is the ONE owner of child paint order (the Vello scene, the render IR and `hit_test` all walk it, so the layer you see on top is the layer you click), `Action::StackingMenu` / `SetCanvasStacking` are the panel's control | `canvas_stacking_reverses_the_paint_order_and_never_the_layer_list`, `canvas_stacking_decides_which_layer_paints_on_top`, `the_hit_test_follows_canvas_stacking`, `the_canvas_stacking_menu_writes_figmas_two_orders` |
| **Sections** are a labelled container on the canvas: place one with the Section tool (`⇧S`) — "Click and drag the location of the canvas where you'd like the section to go" — or wrap the selection in one (right-click → **Wrap in new section**); a selection inside a frame or a group is **lifted** to the canvas with its place kept, because "sections … cannot be contained within frames or groups"; a section **takes in** the layers it is dragged or drawn over; `Delete` removes the section **and its contents**, `⌘⌫` / `Ctrl+Backspace` deletes it **without** them | [Organize your canvas with sections](https://help.figma.com/hc/en-us/articles/9771500257687) | `x_editor::Editor::section_selection` is the one wrapper (in place when the parent is a Section or the page, `lift_into_section` otherwise — page positions first, the section drawn around them, each member moved by the difference), `section_absorb` owns "the section takes what it fully covers" (locked and rotated layers stay out), `delete_keeping_contents` is `⌘⌫`; `NodeKind::Section` cannot be inserted into a Frame or a Group (`Editor::insert_nodes`, and the reorder command refuses the move), and `Tool::Section` draws on the canvas whatever container is under the drag | `a_section_lifts_layers_out_of_a_frame_and_keeps_their_place`, `a_section_never_lands_inside_a_frame_or_a_group`, `a_section_takes_in_the_layers_it_covers`, `deleting_a_section_can_keep_its_layers`, `the_section_tool_is_shift_s_and_shares_the_frame_slot`, `the_section_tool_draws_on_the_canvas_and_takes_what_it_covers`, `the_canvas_menu_wraps_a_selection_in_a_section` |

| **Rotate on canvas**: *"Hover just outside one of the layer's bounds until the icon appears. Click and drag to rotate your selection"* and *"Hold down Shift to snap rotation values to increments of 15"*; the pivot is *"the horizontal and vertical center of the current selection … by default"*, `⌥R` *"reveal[s] the rotation origin"* and dragging that target moves it, and the panel's field counts *"going 15° past 180° … an angle of -165°"* | [Adjust alignment, rotation, position, and dimensions](https://help.figma.com/hc/en-us/articles/360039956914) | `state::rotate_corner_at` is the ONE owner of the zone (outside the bounds, starting past the resize handle's `HANDLE_TOL`), `state::rotation_pivot` of the point turned about, `Action::ToggleRotationOrigin` + `Drag::RotationOrigin` of `⌥R`; `x_core::normalize_degrees` owns the angle convention, `Editor::set_selection_rotation` / `rotate_selection_from` are the writers and `Transform::rotate_about` does the orbit | `the_rotate_ring_turns_the_selection_about_its_centre`, `shift_snaps_a_canvas_rotation_to_fifteen_degrees`, `option_r_moves_the_rotation_origin_and_the_pivot_follows`, `rotating_a_selection_orbits_every_layer_about_the_pivot`, `a_layers_own_origin_is_the_pivot_of_its_rotation`, `the_angle_convention_counts_back_down_past_180` |

| **Masks**: *"Masks sit below the layers they affect"* and the mask is *"any layer"* — it clips the siblings above it until the next mask or the parent's edge — while the design panel's **Mask** section switches its **type**: *Alpha* (the default, *"based on the opacity of the mask"*), *Vector* (*"the shape … any translucency is ignored"*) and *Luminance* (*"the brightness of the mask"*, white shows and black hides) | [Masks](https://help.figma.com/hc/en-us/articles/360040450253-Masks) | `x_core::MaskType` is the ONE owner of the three types (the dropdown, the file's `maskType` key and the renderer all read it); `Editor::use_as_mask` is the writer behind `⌘⌥M`, the canvas menu's **Use as mask** row and the sidebar row — a multi-selection becomes the mask object Figma creates, floor-to-ceiling in ONE undo entry, and the same gesture on a mask object clears it — and `Editor::set_mask_type` is the Mask section's own writer, reaching a mask object's mask through `mask_section_target`; the IR's mask scope scales the masked layers by the mask's own alpha (Alpha) or luminance (Luminance) and by nothing for Vector, and `mask_path_of` falls back to the layer's bounds so text, images and groups can be masks too. **Not built:** per-pixel masking (a blurred, gradient or image mask clips hard), *View → Mask outlines*, the layers-panel mask glyph and the arrows over the masked layers | `use_as_mask_makes_the_bottom_layer_the_mask`, `use_as_mask_toggles_one_layer_and_its_type_is_undoable`, `mask_types_scale_the_masked_scope`, `the_mask_shortcut_masks_the_bottom_layer`, `the_mask_section_switches_the_masks_type`, `the_selection_menu_offers_use_as_mask` |
| **Place image**: *"Select Image/video from the Shape tools menu … or use the keyboard shortcut ⇧⌘K"*, *"Click on the canvas to place the image … using its original dimensions"*, *"Select an existing object on the canvas to replace its fill with the image"*, *"To discard any remaining images or videos, press Delete"* | [Add images and videos](https://help.figma.com/hc/en-us/articles/360040028034-Add-images-and-videos-to-designs) | `Tool::PlaceImage` + `App::placing_images` are the cursor and its queue; `Host::place_images` registers the bytes (`Assets::register`, content-hashed, `probe_dimensions` for the size) and either fills the standing selection or arms the tool; `finish_create` places a click at the file's own size — capped at `PLACE_MAX_DIM`, Figma's 4096 px upload cap — and fills whatever the click lands on, while a drag draws the image at the box you drew; `Editor::set_image_asset` is the ONE writer that swaps an image layer's picture and keeps its fit mode, focal point, scale and flips; `App::place_image_taken` takes one file out of the queue per placement and hands the cursor back to Select with the last; Esc drops the placement and `Delete` discards the rest. **Not built:** the crop mode of the same article (handles, **Aspect ratio**, **Resize to fit**, the `⌘`-drag quick crop), **Place all**, the cursor's count badge, HEIC/TIFF/video and OS file-drop | `the_place_image_tool_places_at_the_click_and_sizes_by_drag`, `placing_an_image_fills_the_selected_shape_or_swaps_the_picture`, `a_click_scales_a_file_bigger_than_figmas_cap`, `escape_drops_a_pending_image_placement`, `the_palette_lists_place_image_with_its_shortcut`, `set_image_asset_swaps_the_picture_and_keeps_the_crop` |
| **Crop an image**: *"Double-click the image layer to enter crop mode"*, *"Aspect ratio is maintained by default when cropping"*, *"Option/Alt also modifies the opposite sides"*, *"Click on the canvas or press Enter to apply your changes"*, and **Resize to fit** | [Crop an image](https://help.figma.com/hc/en-us/articles/360040675194) | `App::crop` (`CropSession`) is the mode and its one-entry ledger; `App::begin_crop` is the ONE way in — the double-click and the fill mode becoming **Crop** both call it, and it switches the fill mode to Crop; `state::crop_placement_from` is the ONE crop resolution — in the layer's own space, the picture scales about the corner opposite the one being held (⌥ → the frame's centre), one zoom for both axes, and the image pixel under the anchor does not move — while `crate::state::crop_corners`/`crop_corner_at`/`crop_inside` own the frame's hit geometry; `Editor::set_image_placement` and `Editor::set_image_fit` are the writers (one entry each, no-op refused) and `Editor::fit_image_to_picture` is **Resize to fit**; `App::crop_apply` merges the session into ONE undo entry, `App::crop_cancel` writes the placement and the fill mode back, and `paint_crop_chrome` owns the frame, its handles, the picture's edges and the zoom chip. **Not built:** the faded uncropped surround, Control's free aspect, the **Aspect ratio** picker, the crop-value slider, the `⌘`-drag quick crop, and rotate/resize inside the mode | `a_corner_drag_crops_about_the_opposite_corner`, `escape_puts_the_cropped_picture_back`, `dragging_inside_the_crop_frame_repositions_the_picture`, `resize_to_fit_makes_the_layer_the_size_of_the_picture`, `the_crop_frame_answers_its_corners_and_its_inside`, `the_image_crop_writers_are_one_entry_each` |
| Figma flips a layer with **⇧H** / **⇧V** — *"Use the right-click menu to apply a flip transformation, or the keyboard shortcuts: Flip horizontal: ⇧H · Flip vertical: ⇧V"* — and the flip is a transform of the layer, not a re-crop: the article notes Figma keeps the matrix transform on the selection afterwards | [Adjust alignment, rotation, position, and dimensions](https://help.figma.com/hc/en-us/articles/360039956914) | `App::flip_images` toggles `ImagePlacement::flip_h`/`flip_v` through `Editor::set_image_placement` and folds a multi-layer selection into ONE undo entry; the keys are `run.rs::on_character`'s ⇧ arms, which run ahead of `Tool::from_shortcut` so the Hand and the Move tool keep the plain `H`/`V`; the menu rows are `context_menu.rs`'s `FlipHorizontal`/`FlipVertical`, gated on the selection carrying an image; the two buttons sit in the Image section beside Rotate 90°. **Not built:** flipping a vector or a group — the engine's flip lives on the image placement | `shift_h_and_shift_v_flip_the_image_about_its_axis`, `the_flip_covers_the_selection_in_one_undo_entry`, `the_image_section_flip_buttons_write_the_placement`, `the_selection_menu_offers_flip_rows_for_an_image`, `design_mode_shortcuts_resolve` |
| **Corner radius and smoothing**: *"Hover just inside a corner until the white circle icon appears, then drag"*, *"Click Independent corners … in the right sidebar"* to get a value per corner, and *"Click iOS to set corner smoothing to 60%"* | [Adjust corner radius and smoothing](https://help.figma.com/hc/en-us/articles/360050986854) | `state::node_corner_radii` is the ONE reading of a layer's four corners (the stored array when they are independent, the uniform radius otherwise) and `state::radius_handle_at` / `radius_handle_point` own the canvas dot's zone and place (`RADIUS_TOUCH`, `RADIUS_KEEP` and `RADIUS_HANDLE_MIN`); `Editor::set_uniform_radius`, `set_corner_radius` and `set_corner_smoothing` are the writers (one undo entry each, a no-op refused) over `Command::SetCorners`, whose apply clause is literal so an undo restores exactly what the node carried, while `Editor::set_corners` gives a frame's uniform radius four equal corners because a frame has no radius field of its own; `Action::ToggleCorners`, `FieldId::CornerRadius(i)`, `Action::SetCornerSmoothing` and `Action::CornerSmoothingIos` are the panel's, `Drag::RadiusCorner` (⌥ = that corner alone, rectangles only) and `Drag::CornerSmooth` the canvas's, and `paint_corner_popover` + `paint_canvas_overlays` own the four fields, the slider and the dot. **Not built:** the **Apply variable** slot on each corner field, a typeable smoothing value (slider + `iOS` only), ⌥-drag on a frame's corner, the Small/Big nudge keys on the radius, and per-point radius in vector-edit mode | `the_corner_panel_writes_one_corner_at_a_time`, `the_ios_chip_sets_corner_smoothing_to_sixty`, `the_radius_handle_sits_on_the_corner_arc`, `dragging_the_canvas_corner_handle_rounds_the_shape`, `corner_radius_extends_to_frames_and_smoothing_is_one_entry` |

| **Bulleted and numbered lists**: *"You can add a bulleted or numbered list to any text layer"* — created by typing `-` / `1.` + `Space`, or with the **List style** property in the type details; `⌘⇧8` makes a bulleted list and `⌘⇧7` a numbered one; *"changes to the weight of your text will apply to the bullet or the counter"*; **No list** removes the styling | [Create bulleted and numbered lists](https://help.figma.com/hc/en-us/articles/360040449773) | `x_core::ListStyle` + `list_marker` are the ONE owner of the three styles and the marker each item carries; `TextBlockStyle::list` takes the marker column (`LIST_MARKER_GAP`) off the wrap width, indents the text into it and shapes the bullet or the 1-based counter in the line's own ink — inside `glyph_outlines`, so the canvas, the raster sink, the PDF sink and the SVG exporter all get the same list, and `TextLayoutKey::list` keeps a bulleted block from being served a plain cached layout; `Editor::set_list_style` is the writer (no-op refused) behind the picker and the two shortcuts, whose repeat press removes the style | `the_list_style_picker_writes_the_layer_and_its_render_tree`, `a_list_widens_the_auto_box_by_its_marker_column`, `a_list_item_gets_a_marker_column_and_a_counter` |
| **Adjust text dimensions**: **Auto width** *"lets the text layer grow horizontally"*, **Auto height** wraps, **Fixed size** pins both — and *"When you manually change a layer's dimensions in the canvas, Figma will also update the resizing property to Fixed size"*, while the handle gesture hands it back to Auto width | [Adjust text dimensions and resizing](https://help.figma.com/hc/en-us/articles/27378154668951) | The `tm` binding is the ONE owner of the resizing property (absent = auto width); every canvas path that resizes a text layer by hand writes `tm=fixed` (the corner drag, the marquee scale and the numeric field), `Host::fit_text_to_content` is the way back (it clears `tm`, re-fits the box through `autosize_text_node`, whose box carries the marker column too), the second press on a text layer's box handle sets it going (`Host::fit_text_at`, guarded by the double-click window) and the Layout section's **Resizing** chip shows and flips the two modes (`Action::ToggleTextResize`). **Not built:** Auto height as a third mode | `a_manual_resize_pins_a_text_layer_and_the_handle_gesture_fits_it` |
| **The keyboard's last rows**: `⇧E` toggles the **Design** and **Prototype** tabs; `⌘R` renames the selected layer; `⇧A` adds auto layout; `⌥⌘K` creates a component (`⇧⌘K` is *Place image*); `N` / `⇧N` zoom to the next / previous frame and wrap; `⌘\` hides the UI while `⇧⌘\` hides the **left panel only**; `⌘/` is Quick Actions; `⇧?` opens the keyboard-shortcuts panel (`⌃⇧?` on the help page — this host reports the character, not the Control key) | [Keyboard shortcuts](https://help.figma.com/hc/en-us/articles/360040328653-Keyboard-shortcuts), [Guide to prototyping](https://help.figma.com/hc/en-us/articles/360040314193-Guide-to-prototyping-in-Figma) | `Host::toggle_right_tab` is the tab toggle (Inspect is a third tab here and the toggle leaves it alone); `Host::rename_selected_layer` reaches the same `App::begin_layer_rename` the layers panel's double-click does; `⌥⌘K` goes through `CtxCmd::MakeComponent`, the context menu's own path; `⇧A` dispatches the panel's `Action::AddAutoLayout`; `Host::zoom_to_frame` walks the page's top-level frames and fits each through `zoom_to_rect`, which is also ⇧2's fit (one owner for the arithmetic); `ui_minimized` and `left_minimized` are two states behind the two hide keys, and the left dock yields its width in one place (`App::editor_regions`); `Host::toggle_palette` is the one palette opener behind `⌘K` and `⌘/`; `paint_shortcuts_panel` paints the sheet and pushes its full-window scrim last, so the reverse scan finds the scrim before the canvas. The vector eraser — ours, Draw-only per rows 1.19 / 20.5 — moved to the plain `E` rather than keep Figma's `⇧E` | `shift_e_toggles_the_tabs_and_the_eraser_keeps_its_own_key`, `the_layer_keys_are_figmas_rename_auto_layout_and_component`, `n_walks_the_pages_frames_in_order`, `the_shortcut_sheet_and_the_two_hide_ui_keys` |

| **Measure with ⌥**: select a layer, *"hold down the modifier key"* (⌥) and *"hover over the second object"* — *"Figma will display a red line between the two objects, as well as horizontal and vertical measurements"*; `⌘⌥` reaches a layer nested inside the one under the cursor | [Measure distances between layers](https://help.figma.com/hc/en-us/articles/360039956974-Measure-distances-between-layers) | `App::measure_spans` is the ONE answer to "what is being measured right now" — one selected layer against `hover_node`, the layer the canvas already outlines, and nothing at all unless ⌥ is held with no drag, no field and no inline edit open; `state::measure_between` is the geometry (each axis between the edges the pair FACES, anchored on the band the two share, and no line at all where the projections overlap — Figma draws none there), `state::band_mid` the anchor, and `crate::world_rect_of` the layer's WORLD bounding box, so a nested or rotated layer measures where it is drawn; `paint_measure` draws that list and nothing else — the red line, its end ticks and the value at the middle — and the hover outline takes the same ink while ⌥ is held. **Not built:** the drag-time spacing numbers Figma shows while moving a layer, and measuring between two guides (the article's own note) | `option_measures_the_gap_to_the_layer_under_the_cursor`, `measure_reads_both_axes_of_a_diagonal_pair`, `a_shared_band_anchors_the_line_and_an_overlap_reads_nothing` |

| **Move while resizing**: hold `Space` *"in the middle of another action"* to move the box and *"release Space to return to the previous action"* — the shortcut tables list it as **Move while resizing** | [Edit vector layers](https://help.figma.com/hc/en-us/articles/360039957634-Edit-vector-layers) (the rule; the design-file shortcut tables carry the row) | `Drag::ResizeSel` gained the two riders that make this possible: `space` — the pointer the last move-with-`Space` event landed on, `None` until the first one, so the box never jumps on the way in — and `offset`, how far that move has carried the box. While `Space` is held the travel is `smart_move`, the call the plain layer drag uses, so one snapping rule serves both gestures, and the size is left exactly as the resize gave it; when `Space` is released `offset` is added to the drag's `orig` and `start`, so the resize re-bases on the box's new place while the pointer keeps its grip on the corner it took hold of. Until `Space` is ever used the offset is zero and the arithmetic is byte-for-byte what it was | `space_moves_the_box_mid_resize_and_the_resize_resumes_from_there` |

| **Advanced stroke settings**: *"navigate to the **Stroke** section in the right sidebar and select **Advanced stroke settings**"* — **Stroke style** (Solid / Dashed / Custom), the Dashed style's **Dash**/**Gap**, the Custom **Dashes** pattern (*"dash, gap, dash, gap..."*), the three **joins** with a **Miter angle**, and the **Start point** / **End point** menus | [Apply and adjust stroke properties](https://help.figma.com/hc/en-us/articles/360049283914-Apply-and-adjust-stroke-properties) | the Stroke section's style icon (`ToggleStrokeStyle`) opens `paint_stroke_style_panel`, and the panel's rows come from `state::stroke_panel_rows` — which is also where its height comes from, so the card and its contents are one list and the panel follows the stroke it describes (the Dash/Gap pair only for **Dashed**, the pattern field only for **Custom**, the Miter angle only for a **Miter** join). Every writer goes through `Host::edit_stroke_options`, which materializes the visual stacks, seeds the stroke layer from `stroke` when the layer has none, and SKIPS a layer already carrying the options — so clicking the row a stroke already answers with never lands an undo entry. The style is DERIVED from the pattern (`StrokeStyleKind::of`: empty = Solid, a pair = Dashed, longer = Custom) rather than stored, so no typed pattern can leave the panel claiming a style the layer has not got; the Miter angle the field shows is `state::miter_limit_to_angle`, the inverse of the `1 / sin(angle / 2)` relation the renderer's miter limit is. Every row that shows a line draws it through `paint::stroke_path_options` — the same caps/join/miter/dash construction `x_render::text_geometry::stroke_style` builds for the canvas — and the cap menu's rows are named (None / Round / Square) with the end they paint. **Not built:** Figma's separate **Dash cap**, its **Individual strokes** (per-side) list, and the Dynamic/Brush tabs | `the_advanced_stroke_panel_opens_from_the_section_and_writes_the_styles`, `the_stroke_panel_writes_the_join_the_angle_the_pattern_and_the_ends`, `the_stroke_style_is_what_the_pattern_is`, `the_style_rows_carry_the_numbers_over`, `the_miter_angle_is_the_limit_it_stands_for`, `the_panel_shows_the_rows_the_stroke_has_settings_for`, `the_join_and_cap_rows_are_figmas_names` |
| **Outlines mode (⌘Y)**: *"Show outlines — to toggle outlines on and off, ⌘Y"* — with it on, the canvas renders each layer as a wireframe: fills, images, blends and effects are not painted, only each layer's outline | [Designlab Figma 101 — Tips and Tricks](https://designlab.com/figma-101-course/tips-and-tricks) | `x_render::outline_view(root, width)` is the ONE strip — per node the fill, stroke and effect stacks clear, the blend goes back to Normal and the stroke becomes a solid hairline in `OUTLINE_COLOR` at `width`; Image and Text nodes become the plain box they own (both paint themselves and would swallow the stroke), children are stripped recursively so an instance resolves from the stripped registry, and the render root (the page — not a layer) keeps no outline of its own; `Action::ToggleOutlines` sits on ⌘Y (the old redo arm — redo keeps Figma's ⇧⌘Z) with the status line and the shortcuts-panel row; `Host::canvas_scene` renders the copy at a `1.0 / zoom` hairline — a render mode, so the document, its undo history and the exporters are untouched. **Not built:** Figma renders text as glyph outlines; we render the text layer's box | `cmd_y_toggles_outline_view_and_never_touches_the_document`, `outline_mode_hairs_the_canvas_and_paints_no_image`, `the_shortcut_sheet_carries_the_outline_view_key`, `outline_view_is_a_wireframe_copy_of_the_document`, `outline_view_strips_the_instances_master_too`, `outline_mode_paints_only_the_wireframe` |

## 3. Open — claimed, or fixed, but not pinned yet

The list is the point: an unpinned claim is visible instead of implied, and the
guard counts it.

| behaviour | Figma source | our rule (owner) | pinned by |
| --- | --- | --- | --- |

## 4. Running it

```
node tools/design-sheet/guard.mjs          # 6 checks: ceilings, one owner, icons, parity
node tools/design-sheet/check.mjs          # the sheet's own assertions (jsdom-free)
node tools/design-sheet/check_screens.mjs  # the screen assertions (needs jsdom)
bash scripts/check.sh                      # the gate: all of the above plus fmt, clippy, tests, docs
```

`scripts/check.sh` runs the guard, so a duplicated canvas colour, an unknown icon
name, or a parity row whose test was renamed fails a push instead of reaching the
owner's screen.

The gate cannot see the screen, so the *visual* half of the design system runs as
its own CI job, `Screenshots (software Vulkan)`: it installs lavapipe (Mesa's
software Vulkan driver), runs the six `#[ignore]`d GPU/screenshot tests, and
uploads the PNGs they write (`apps/x-designer/screenshots`, plus the loading
previews the job tells `loading_screen_visuals` to write with
`X_NATIVE_LOADING_PREVIEW_DIR`) as the `screenshots` artifact. A design
regression is then a picture in the run, not a sentence in a report.
